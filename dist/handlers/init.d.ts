import type { InitializeToyboxParams, InitResult } from '../types.js';
/**
 * Initialize a new TOYBOX repository with GitHub Pages publishing
 */
export declare function initializeToybox(params: InitializeToyboxParams): Promise<InitResult>;
